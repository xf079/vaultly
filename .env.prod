# ============ 应用 ============
NODE_ENV=development
PORT=3000
API_PREFIX=api

# ============ 数据库 ============
# SQLite（默认内存库，生产可改为文件路径）
DATABASE_URL=:memory:
# 文件示例: file:./data/vaultly.db

# ============ JWT ============
# 生产环境务必更换为随机强密钥
JWT_SECRET=your-super-secret-key-change-in-production
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d
JWT_ISSUER=vaultly
JWT_AUDIENCE=vaultly-client

# ============ Redis ============
REDIS_URL=redis://localhost:6379
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD=
REDIS_DB=0
REDIS_KEY_PREFIX=vaultly:

# ============ Swagger ============
SWAGGER_PATH=api-docs
SWAGGER_TITLE=Vaultly API
SWAGGER_DESCRIPTION=Vaultly 密码管家 - 企业级 API
SWAGGER_VERSION=1.0.0
